import 'package:flutter/material.dart';

class AdminIncentive extends StatelessWidget {
  const AdminIncentive({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
